# Use an open port if 8000 is occupied by another Laravel project (e.g. 8090)
VITE_API_URL=http://127.0.0.1:8000/api

# Session limits (must stay in sync with backend SANCTUM_* values)
VITE_SESSION_IDLE_MINUTES=15
VITE_SESSION_LIFETIME_MINUTES=480
